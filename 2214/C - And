#include <bits/stdc++.h>
#include <vector>
using namespace std;
#define ll long long
#define all(a) a.begin(), a.end()
 
int main() {
    int t;
    cin>>t;
    int a;
    while(t--){
      ll a1, b, c;
    if (!(cin >> a1 >> b >> c)) return 0;
 
    vector <ll> a(3);
    a[0] = a1;
    a[1] = b;
    a[2] = c;
 
    sort(all(a));
    ll med = a[1];
 
    
    ll result = ((a1 ^ b) ^ c) - med;
 
    cout << result << endl;
      
    
    }
    
    return 0;
}